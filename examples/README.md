# Examples

This directory contains public examples that can be referenced from README files and release notes.

- `screenshots/kiosk-desktop.png`: current long-bar kiosk layout at `3840x1100`.
- `screenshots/kiosk-mobile.png`: narrow browser preview.

Local-only screenshots should stay in `screenshots/local/`, which is ignored by Git.
